{
  "name": "knltb-rating-plugin",
  "private": true,
  "description": "Browserextensie die de KNLTB enkel- en dubbelrating achter spelersnamen zet op mijnknltb.toernooi.nl",
  "license": "MIT",
  "scripts": {
    "test": "node test/run.js",
    "pack": "git archive --format=zip -o knltb-rating.zip HEAD -- manifest.json icons \":(glob)*.js\" \":(glob)*.css\" \":(glob)*.html\""
  },
  "devDependencies": {
    "jsdom": "^26.1.0"
  }
}
